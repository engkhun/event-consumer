spring:
  cloud:
    function:
      definition: orderingEventConsumer;orderingEventProducer
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        eventConsumer-in-0:
          destination: data_stream
        eventProducer-out-0:
          destination: data_stream